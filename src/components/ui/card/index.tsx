import React from 'react';

export const Card = ({}: {
	key: string | number;
	children: React.ReactNode;
	className: string;
}) => {
	return <div>card</div>;
};

export const CardContent = ({}: {
	children: React.ReactNode;
	className: string;
}) => {
	return <div>card</div>;
};

